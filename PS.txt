#include <bits/stdc++.h>
using namespace std;

//STD_TYPES
typedef  unsigned char                     u8;
typedef  signed   char                     s8;
						                   
typedef  unsigned short int                u16;
typedef  signed   short int                s16;
						                   
typedef  unsigned long int                 u32;
typedef  signed   long int                 s32;
						                   
typedef  float                             f32;
typedef  double                            d64;
typedef  long long                         ll ;

#define  LOOP(n)         for(s32 i ; i<n ; i++)
//To increase the speed of cout
#define  endl                              '\n'  

        

int main()
{
    //to increase the speed of cin
    cin.tie(0);
    cin.sync_with_stdio(0);
    //Gurad of Header file 
    #ifndef  ONLINE_JUDGE
    freopen("input.txt","r",stdin);
    #endif
    
    return 0;
}
